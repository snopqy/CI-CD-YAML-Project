---
apiVersion: v1
kind: Service
metadata:
  name: nginx-nodeport
  labels:
    app: nginx
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - name: http
      port: 80        # service port
      targetPort: 80  # container port
      nodePort: 30080 # allowed range 30000–32767
